CREATE SCHEMA `db_ss` ;
COMMIT;

CREATE TABLE `db_ss`.`t_pedido_oracao` (
  `CD_PEDIDO_ORACAO` INT NOT NULL,
  `NM_USUARIO` VARCHAR(45) NULL,
  `DS_EMAIL` VARCHAR(80) NULL,
  `DS_PEDIDO_ORACAO` VARCHAR(3000) NULL,
  `DT_PEDIDO` DATE NULL,
  PRIMARY KEY (`CD_PEDIDO_ORACAO`));
COMMIT;

ALTER TABLE `db_ss`.`t_pedido_oracao` 
CHANGE COLUMN `CD_PEDIDO_ORACAO` `CD_PEDIDO_ORACAO` INT(11) NOT NULL AUTO_INCREMENT ;
commit;

CREATE TABLE `db_ss`.`t_usuario_adm` (
  `NM_USUARIO` VARCHAR(20) NOT NULL,
  `DS_SENHA` VARCHAR(45) NOT NULL,
  PRIMARY KEY (`NM_USUARIO`));
COMMIT;

INSERT INTO t_usuario_adm (NM_USUARIO, DS_SENHA) values 
('administrador','graçadeDeus');
commit;